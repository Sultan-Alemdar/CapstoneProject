<Page
    x:Class="DesktopApp.Views.RemoteConnectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="DarkSlateGray"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    DataContext="{x:Bind ViewModel}"
    xmlns:v="using:DesktopApp.Views"
    mc:Ignorable="d">

    <Grid  x:Name="ContentArea">
        <Grid.Resources>
            <Storyboard x:Name="AppBarClose" >
                <DoubleAnimation Storyboard.TargetName="AppBarGrid" FillBehavior="HoldEnd" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransForm.X)" From="0" To="-427">
                    <DoubleAnimation.EasingFunction>
                        <CircleEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>

            </Storyboard>
            <Storyboard x:Name="AppBarOpen" >
                <DoubleAnimation Storyboard.TargetName="AppBarGrid" FillBehavior="HoldEnd" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransForm.X)" From="-427" To="0">
                    <DoubleAnimation.EasingFunction>
                        <CircleEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>

        </Grid.Resources>
        <MediaElement
            Name="PeerVideo" RealTimePlayback="True"
            RelativePanel.AlignLeftWithPanel="True"
            RelativePanel.AlignRightWithPanel="True"
            RelativePanel.AlignTopWithPanel="True"
            RelativePanel.AlignBottomWithPanel="True"
            MediaFailed="PeerVideo_MediaFailed" />

        <Grid  x:Name="AppBarGrid"  HorizontalAlignment="Left" VerticalAlignment="Top" >
            <Grid.RenderTransform>
                <TranslateTransform x:Name="Translate"  ></TranslateTransform>
            </Grid.RenderTransform>

            <Border  x:Name="Border" Margin="20,15,0,0"  BorderThickness="2" BorderBrush="White" CornerRadius="17">
                <StackPanel Margin="10" CornerRadius="17" Height="Auto" Width="Auto" Orientation="Horizontal">

                    <AppBarButton  Icon="GoToStart" Label="Başlat" />
                    <AppBarButton  Icon="Stop" Label="Durdur" Visibility="Visible" IsEnabled="False"/>
                    <AppBarButton Icon="Clear" Label="Sonlandır"/>
                    <AppBarSeparator />
                    <AppBarButton Icon="Keyboard"  Label="Yakala"/>
                    <AppBarButton Icon="Orientation" Label="Yönlendir"/>
                    <AppBarSeparator  />
                    <AppBarButton Icon="ClosePane" Label="Kapat" Click="CloseButton_Click" Visibility="{x:Bind CloseButonVisibility, Mode=OneWay}"/>
                    <AppBarButton Icon="OpenPane" Label="Aç"  Click="OpenButton_Click" Visibility="{x:Bind OpenButtonVisibility, Mode=OneWay}"/>


                </StackPanel>
            </Border>
        </Grid>

    </Grid>

</Page>
