<Page
    x:Class="DesktopApp.Views.RemoteConnectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="DarkSlateGray"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    DataContext="{x:Bind ViewModel}"
    xmlns:v="using:DesktopApp.Views"
    mc:Ignorable="d">

    <Grid  x:Name="ContentArea">
        <Grid.Resources>
            <Storyboard x:Name="AppBarClose" >
                <DoubleAnimation Storyboard.TargetName="AppBarGrid" FillBehavior="HoldEnd" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransForm.Y)"  To="-20">
                    <DoubleAnimation.EasingFunction>
                        <CircleEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>

            </Storyboard>
            <Storyboard x:Name="AppBarOpen" >
                <DoubleAnimation Storyboard.TargetName="AppBarGrid" FillBehavior="HoldEnd" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransForm.Y)" To="0">
                    <DoubleAnimation.EasingFunction>
                        <CircleEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>

        </Grid.Resources>
        <MediaElement
            Name="PeerVideo" RealTimePlayback="True"
            RelativePanel.AlignLeftWithPanel="True"
            RelativePanel.AlignRightWithPanel="True"
            RelativePanel.AlignTopWithPanel="True"
            RelativePanel.AlignBottomWithPanel="True"
            MediaFailed="PeerVideo_MediaFailed" />
        <Border Width="200" Height="200" BorderThickness="20" BorderBrush="Red" PointerEntered="Border_PointerEntered"></Border>
        <Grid  x:Name="AppBarGrid"  Background="Red" HorizontalAlignment="Center" VerticalAlignment="Top"  >
            <Grid.RenderTransform>
                <TranslateTransform x:Name="Translate"  ></TranslateTransform>
            </Grid.RenderTransform>

            <Border  x:Name="AppBorder" Margin="20,15,0,0"  BorderThickness="2" BorderBrush="White" CornerRadius="17"  PointerEntered="Border_PointerEntered">
                <StackPanel Margin="10,0,0,10" CornerRadius="17" Height="Auto" Width="Auto" Orientation="Horizontal" >

                    <AppBarToggleButton Label="aslşkdak" Icon="Accept"></AppBarToggleButton>
                    <AppBarToggleButton Label="aslşkdak" Icon="Accept"></AppBarToggleButton>
                    <AppBarButton  Icon="GoToStart" Label="Başlat" Click="Button_Click" />
                    <AppBarButton  Icon="Stop" Label="Durdur" Visibility="Visible" IsEnabled="False"/>
                    <AppBarButton Icon="Clear" Label="Sonlandır"/>
                    <AppBarSeparator />
                    <AppBarButton Icon="Keyboard"  Label="Yakala"/>
                    <AppBarButton Icon="Orientation" Label="Yönlendir"/>
                    <AppBarSeparator  />
                    <AppBarButton Icon="UnPin" Label="Kapat" Click="UnpinButton_Click" Visibility="{x:Bind UnPinButtonVisibility, Mode=OneWay}"/>
                    <AppBarButton Icon="Pin" Label="Sabitle"  Click="PinButton_Click" Visibility="{x:Bind PinButtonVisibility, Mode=OneWay}"/>


                </StackPanel>
            </Border>
        </Grid>
        <Button x:Name="btn" Content="Button" HorizontalAlignment="Left" Height="105" Margin="364,0,0,0" VerticalAlignment="Top" Width="817" Click="Button_Click"/>


    </Grid>

</Page>
